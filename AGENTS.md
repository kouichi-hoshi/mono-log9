# READ ME

## 開発の基本方針

- 本プロジェクトはテスト駆動開発で実装する
- 開発作業は各サブエージェントに作業を移譲し実行する
- 開発作業は作業計画書に従って進行する

## 開発サイクル

- フェーズ1: UIの実装
  - UIのみ実装する。機能はスタブもしくはモックで代用する。
- フェーズ2: 機能実装
- フェーズ3: 認証・認可、データベース実装
- フェーズ4: E2Eテスト

## テスト実装方針（現段階）

- 本プロジェクトの現段階では、主に「単体テスト + Playwrightを用いたUIテスト」で品質を担保する。
- E2Eテストという語は広義のため、以下の3層で定義して運用する。
  - Unit（単体テスト）:
    - 関数・モジュール単位のロジック、バリデーション、データ変換を検証する。
  - Playwright UIテスト:
    - UI操作起点で、単体テストだけでは検知しづらい不具合（入力→送信→保存/更新→表示反映）を検証する。
    - 画面操作と実装のつなぎ込み漏れ、Server Action境界、フォーム送信経路の不備を重点的に検知する。
  - Full E2E（フェーズ4）:
    - 本番相当構成での総合シナリオを検証する最終フェーズ。

- テストケースごとの使い分けは次を原則とする。
  - まずUnitで満たせるかを検討し、満たせるものはUnitで実装する。
  - 次のいずれかに該当する場合はPlaywright UIテストで補完する。
    - UI操作の連続（入力、選択、ボタン押下、遷移）を伴う。
    - 単体テストでは再現しにくい画面統合起因の不具合が想定される。
    - 回帰防止のため、実際のユーザー導線での確認が必要である。

- 実装は作業計画書に沿って段階的に進め、機能追加時に必要なUnit/Playwright UIテストを同時に追加する。
- フェーズ4では、それまでに追加したPlaywright UIテストを土台として、総合E2E観点を拡充する。

## サブエージェントとSkillを活用する

- サブエージェントとSkillの格納場所
  - /.codex/agents
  - /.codex/skills

### サブエージェントの役割

- director
  - ドキュメント沿って、実装（programmer）とテスト設計（tester）を正しく進行させる
- tester
  - テストケースの作成・テストの評価
- ui-builder
  - UIを実装する専門家
- programmer
  - テスト駆動開発による機能開発

## 実装の進め方

- 運用サイクル
  1. director: ドキュメントを把握し、testerに作業指示を出す
  2. tester: テストケースを作成する
  3. ui-builder: UIを実装する
  4. programmer: テストケースに基づき、TDD、Red → Green → Refactorの順に実装する
  5. tester: programmerが作成・実施したテストコード、実行結果を確認する
  6. director: ドキュメントやプランに沿って実装ができているか確認する
- 各運用サイクルで不備が見つかった場合は作業したサブエージェントに差し戻し、差し戻されたサブエージェントは作業を実行し、完了したら次の運用サイクルに移行する

## ドキュメント

開発に必要な資料は以下に格納されている。

- docs/manage
- docs/specs
- docs/designs
- docs/memo
- docs/plan
- docs/tech-stack
- docs/wireframes
- docs/old-docs

## 禁止事項

以下について、AIの判断で勝手に実行してはならない。実行が必要な場合はユーザーの許可を必ず得る。ユーザーから求められれた場合でもいったん拒否し、リスクを伝えた後に再度実行の是非を確認する。

- env
  - .env / .env.* / **/.env* の内容取得・チャット、ログへの出力、コミットは禁止
- データベース
  - データベースの削除
  - スキーマのマイグレーション実行
  - DBデータの作成・更新・削除（INSERT / UPDATE / DELETE）
- 秘匿情報
  - APIキー、トークン、パスワード等のハードコード
