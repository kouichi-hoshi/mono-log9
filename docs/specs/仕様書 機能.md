---
title: 仕様書 全体
source:
author:
  - 
published:
created: 2026-02-06
description:
tags:
---
# システム構成

- パッケージマネージャー
	- pnpm
- アプリ
    - Next.js
    - tiptap
	    - 投稿エディタのリッチテキスト入力に使用。投稿本文は tiptap の JSON 形式で DB に保存する
    - prisma
    - dayjs
	    - 投稿の作成日・更新日の日時フォーマット表示に使用（表示フォーマット: `YYYY-MM-DD HH:mm`）
    - TanStack Query
	    - 投稿一覧のキャッシュ・再検証・無限スクロールのデータ取得に使用（詳細は `docs/specs/仕様書 投稿一覧取得-キャッシュ-ページング.md` を参照）
- デザインシステム
	- storybook（検討中、初期リリース時は導入しない）
- テスト
    - Jest（単体テスト、結合テスト）
    - Playwright（E2Eテスト）
- UI
    - shadcn/ui
    - tailwindcss
- アイコン
	- https://lucide.dev/
- DB
    - 種類とバージョン: PostgreSQL v17
    - ローカル: ローカル用PostgreSQL
    - 検証用: Neon
    - 本番用: Neon
- 認証
	- Auth.js
		- 項番26（Googleログイン本実装）の詳細仕様は `docs/specs/仕様書 Auth.js（Googleログイン）導入.md` を正とする
- ホスティング
	- Vercel

# 基本仕様

- SPA
		- 画面状態（表示切替/ごみ箱/絞り込み）は URL パラメータで切り替える
			- URL パラメータとして扱うのは `view`（`memo` / `note` / `trash`）と、お気に入り絞り込み（`favoriteMemo` / `favoriteNote`）を基本とする
			- 項番22-Aでノートモーダル履歴連動を導入した後は、ノートモーダル状態を `noteComposer`（`create` / `edit:<postId>`）で扱う
			- `mode` は投稿データの種別（`memo` / `note`）を表す内部概念であり、URL パラメータとしては扱わない
			- 表示中の投稿種別は `view` から導出する（`memo`→`activeMode=memo`、`note`→`activeMode=note`、`trash`→`activeMode=null`）
		- モード切替やごみ箱表示の切替では、取得済みの投稿一覧が即時に復元される（体感の速さを優先）
			- 実装設計は `docs/designs/投稿一覧取得-キャッシュ-ページング_設計.md` を参照
- ブラウザ
	- 以下を対象とする
		- PC用Webブラウザ（Chrome, Firefox, Safari, Edge の最新版）
		- スマートフォン用Webブラウザ（iOS Safari, Android Chrome の最新版）
- スマートフォンのホーム画面追加
	- ホーム画面追加（PWA）に対応する（manifest を用意する）
	- 起動時はアプリっぽく起動する（standalone）
	- 具体のキー/アイコン/テーマカラー等の設計は `docs/specs/仕様書 設定 PWA.md` を参照する
- デザイン・レイアウト
		- 画面のレイアウトは別途資料（Miro）を正とし、スマートフォン相当とPC画面相当のレイアウトで実装する
		- Miro の参照は MCP（`mcp-miro`）経由で行い、ボードIDは設定（`mcp.json`）から取得する（ハードコードしない）



---

# ログインボタン

- ボタン押下でログイン用モーダルウィンドウを表示する
- モーダル内に「Googleでログイン」ボタンを表示する
- ログインボタンを押下すると認証処理を実行する。成功したら、未ログイン用のウェルカムメッセージ／ログインボタンを非表示にし、ログイン中画面に表示を切り替える
	- OAuth成功後の遷移先（`callbackUrl`）は、現在URLのクエリを保持しつつ `stubAuth` のみ除去した同一オリジンURLを使用する（詳細は `docs/specs/仕様書 Auth.js（Googleログイン）導入.md`）
- モーダルの右上のバツボタンを押下するとモーダルが解除される
- 認証に失敗/キャンセルした場合はエラー通知（トースト）を表示し、モーダルは閉じる（再試行は再度ログイン操作から行う。文言は `docs/specs/テキスト・コンテンツ定義.md` を正とする）

---

# ユーザーアイコン

- Google認証想定。googleからユーザーのアイコン画像を取得し表示する
- ログアウト機能
	- アイコンを押下するとポップオーバーが開き、ユーザー名と「ログアウト」を表示する
	- ユーザーアイコンまたはポップオーバー以外のエリアをクリックすると閉じる
	- 「ログアウト」リンクを押下するとログアウトする
		- ログアウト成功時: URL パラメータを全削除して `/` に戻し、未ログイン画面へフェードインで切り替える（ログアウト専用画面は作らない）
			- ログアウト成功時はクライアントキャッシュ（投稿一覧キャッシュ/スクロール保存値）を破棄する（詳細は `docs/specs/仕様書 Auth.js（Googleログイン）導入.md`）
		- ログアウト失敗時: エラー通知（トースト）を表示し、未ログイン画面へは切り替えない（リトライ可能）
	- 項番26時点の非スコープ（後続検討）
		- ログアウト後にブラウザの戻る/進むを実行したときの履歴復元挙動（DOM/BFCache 表示含む）は項番32（Playwright E2E）で検証・確定する
- このアプリについて
	- ユーザーアイコン押下で開くポップオーバー内に「このアプリについて」を表示する
	- 「このアプリについて」を押下するとモーダルを開き、アプリ名とバージョンを表示する
	- アプリ名は `docs/specs/テキスト・コンテンツ定義.md` のサービスロゴ名称（Mono Log）を正とする。実装では `lib/appMeta.ts` の `APP_NAME` を参照してよい
	- バージョンは `package.json` の `version` をビルド時に `NEXT_PUBLIC_APP_VERSION` として注入し、モーダルで表示する（`next.config.ts` の `env` で設定）
	- モーダルは右上の×で閉じる。モーダル外クリックや Esc での閉じる動作は既存モーダル（ログイン、離脱確認等）の挙動に合わせる
	- モーダルタイトル「このアプリについて」および表示文言は `docs/specs/テキスト・コンテンツ定義.md` を正とする（未定義の場合は同ドキュメントに追記する）
- 将来的にユーザー固有機能の設定を追加予定（内容未定）

---

# 表示切り替えUI

- 構成要素
	- 「メモ」ボタン
		- 押下するとメモの投稿一覧が表示される
	- 「ノート」ボタン
		- 押下するとノートの投稿一覧が表示される
	- 「ごみ箱」ボタン
		- 押下するとごみ箱の投稿一覧が表示される 
- 各モードとごみ箱は URL クエリで判定し、ブラウザでの進む/戻るで状態を保存できるようにする
- 各ボタンが押下されるとそのボタンがアクティブになり、それ以外のボタンは非アクティブになる

---

# 投稿一覧

- 本章は要約とし、詳細仕様は `docs/specs/仕様書 投稿一覧.md` を正とする
- 投稿一覧はタイトル、お気に入り絞り込みボタン、一覧表示、各投稿カードの表示要素・操作、無限スクロールで構成する
- 取得・キャッシュ・ページングの技術契約は `docs/specs/仕様書 投稿一覧取得-キャッシュ-ページング.md` を参照する

---

# エディタ

- 本章は要約とし、詳細仕様は `docs/specs/仕様書 投稿編集・離脱確認.md` を正とする
- エディタはメモ用・ノート用を分離して実装し、保存済み投稿のモードは変更しない
- TipTap の入力仕様（ショートカット/ツールバー）は `docs/specs/仕様書 Tiptap.md` を正とする
- 投稿本文の保存形式/バリデーションは `docs/specs/仕様書 投稿編集-本文形式.md` を正とする
- 離脱確認（hasEdits）の判定・トリガー・戻る/進む時の保留制御は `docs/specs/仕様書 投稿編集・離脱確認.md` を正とする
- 離脱確認ダイアログの表示文言は `docs/specs/テキスト・コンテンツ定義.md` を正とする


---

# エディタの出し分け仕様（要約）

- URLクエリ view から画面状態を判定する
- `view=memo` ではメモエディタ、`view=note` ではノートモーダル、`view=trash` ではエディタ非表示とする
- 詳細な判定/モーダル開閉/履歴連動は `docs/specs/仕様書 投稿編集・離脱確認.md` および `docs/specs/仕様書 URLクエリ状態管理.md` を正とする

---
# ごみ箱

- 本章は要約とし、詳細仕様は `docs/specs/仕様書 投稿一覧.md` を正とする
- ごみ箱は投稿一覧の `view=trash` として扱い、一覧表示・個別投稿操作（復元/完全削除）・一括削除UI・削除確認モーダルを提供する
- `view=trash` 時のクエリ評価ルールは `docs/specs/仕様書 URLクエリ状態管理.md`、削除確認モーダル文言は `docs/specs/テキスト・コンテンツ定義.md` を正とする


---

# エラーハンドリング

- 各種アクション、認証・通信エラー時に必要なフィードバックを適切表示する
  - ログイン（OAuth）失敗/キャンセル: トースト表示、モーダルは閉じる（再試行は再度ログイン操作から行う。文言は `docs/specs/テキスト・コンテンツ定義.md` を正とする）
  - ログイン必須操作で 401: トースト表示＋入力維持のままログインモーダル自動表示
  - 403: 環境/設定ガード違反時（例: スタブ禁止環境でスタブ導線を利用）にトースト表示、状態維持
  - 404: 対象不存在/他ユーザー対象/状態不一致時にトースト表示、状態維持（必要なら再取得）
  - 500/予期しないエラー: トースト表示（「サーバーエラーが発生しました」または「エラーが発生しました」）＋状態維持。再試行可能な操作では再試行導線を表示する
  - 投稿APIエラーコードの条件/優先順位は `docs/specs/仕様書 PrismaDB設計.md` の「投稿APIエラー契約（項番29の正本）」を正とする
  - 実装設計の詳細は `docs/specs/テキスト・コンテンツ定義.md`（文言）と `docs/specs/仕様書 スタブ投稿データ（USE_STUB_POSTS）.md`（スタブ運用時のエラー/ガード）を参照

---

# セキュリティ

- Google OAuth による安全な認証
- XSS・SQLインジェクション対策
- 認証・認可エラー時に内部情報を漏洩しない


---

# パフォーマンス

- 体感パフォーマンスを優先し、ローディング表示（スケルトン等）や取得済みデータの即時復元などを活用
- 計測（本番URL）
  - Google PageSpeed Insights（Lighthouse / ラボ）で Core Web Vitals（LCP/CLS/INP）を確認できること
  - Vercel Speed Insights（RUM / フィールド）で Core Web Vitals（LCP/CLS/INP）を確認できること
  - 目標値（MVP, p75）: LCP <= 2.5s / CLS <= 0.1 / INP <= 200ms
