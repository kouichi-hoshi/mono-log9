---
title: F7-8 パフォーマンス計測ベースラインと改善検証
source:
  - /Users/kouichi/project/my_project/mono-log/mono-log9/docs/manage/作業計画書.md
author:
published:
created: 2026-03-03
description: F7-8 の計測手順・結果・改善優先度を記録するテンプレート
tags:
  - memo
  - phase7
  - performance
---
# F7-8 パフォーマンス計測ベースラインと改善検証

## 目的

- 現状実装をベースラインとして再レンダリング回数と主要操作応答を計測し、重さの原因箇所を特定する。
- 改善候補を小さく実施し、同一条件で再計測して効果を確認する。

## 完了条件チェックリスト

- [ ] 計測対象（画面/操作/シナリオ）を定義した
- [ ] 現状Baseline（3回計測、平均/中央値）を記録した
- [ ] 原因候補（コンポーネント/状態/ルーティング）を絞り込んだ
- [ ] 改善サイクル（計測→実装→再計測）を1件以上完了した
- [ ] 再レンダリング回数の比較結果を記録した
- [ ] 主要操作応答（モード切替、モーダル開閉、一覧操作）の比較結果を記録した
- [ ] 結果を `docs/memo` に記録した
- [ ] 改善対象と優先度（High/Medium/Low）を明文化した

## 計測メタ情報

| 項目 | 記録内容 |
| --- | --- |
| 計測日 | 2026-03-03 |
| 実施者 | ユーザー |
| 対象ブランチ/コミット | 未記録 |
| 改善対象ブランチ/コミット | |
| 実行環境（OS/Browser） | Chrome + React DevTools Profiler |
| 計測モード（dev/staging/prod） | dev（ローカル想定） |
| 備考 | 初回記録は各Runの「1 commit目」のみ。全commit（1/m）の合計値は未集計。 |

## 改善サイクル管理（計測→実装→再計測）

| サイクルID   | 対象シナリオ                           | 仮説/狙い                                                  | 実装内容（要約）                                                                                                                                                                                                                                                                                                                                                                     | Baseline                                                   | After                                                                              | 差分                                                          | 判定                        | 実施日        |
| -------- | -------------------------------- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------- | ------------------------- | ---------- |
| CYCLE-01 | PERF-01                          | モード切替時の再描画波及を抑え、total renderを削減する                      | `AuthedScreen` の retry/confirm callback を `useCallback` 化。`Container2`/`HeaderPostArea` を `React.memo` 化。`Container2` の選択トグル処理を `useCallback` 化。                                                                                                                                                                                                                             | `render count=5.7, total render=133.2ms`                   | `render count=5.0, total render=145.8ms`                                           | `render count -0.7, total render +12.6ms (+9.5%)`           | 未達（total render悪化）        | 2026-03-03 |
| CYCLE-02 | PERF-01                          | 一覧描画境界とカード層を最適化し、total renderを改善する                     | `PostsSection` を追加して一覧描画境界を分離。`PostCard`/`TrashPostCard` を `React.memo` 化。`Container2` の map 内 inline callback を I/F更新で削減。                                                                                                                                                                                                                                                   | `render count=5.7, total render=133.2ms`                   | `render count=6.7, total render=157.3ms`                                           | `render count +1.0, total render +24.1ms (+18.1%)`          | 未達（total render悪化）        | 2026-03-03 |
| CYCLE-03 | PERF-01                          | 1回あたりの重い処理（ノートHTML変換/サニタイズ）を描画経路から外し、total renderを改善する | `noteHtmlRenderCache` を追加し `toSanitizableHtml + sanitizeRichHtml` をキャッシュ化。`PostCard` は初期プレーン表示→`setTimeout(0)` で遅延計算し、計算後に `.md-content` を反映。                                                                                                                                                                                                                                | `render count=5.7, total render=133.2ms`                   | `render count=4.0, total render=129.9ms`                                           | `render count -1.7, total render -3.3ms (-2.5%)`            | 未達（KPI未達）                 | 2026-03-03 |
| CYCLE-04 | PERF-01                          | モード切替直後の一括CPU負荷を抑え、スクロール体感を維持しつつtotal renderを改善する      | `noteHtmlIdleScheduler` を追加し `requestIdleCallback` 優先でノートHTML計算を1件ずつ実行。`useNearViewport` を追加して画面外ノートはプレーン表示維持。`Container2` で `noteHtmlEager` を先頭1件のみに制限。                                                                                                                                                                                                                     | `render count=5.7, total render=133.2ms`                   | `render count=4.0, total render=149.3ms`                                           | `render count -1.7, total render +16.1ms (+12.1%)`          | 未達（悪化）。CYCLE-03へロールバック    | 2026-03-03 |
| CYCLE-05 | PERF-01（計測済） / PERF-02 / PERF-03 | ノートモーダル開閉とURL連動更新での不要再描画波及を抑える                         | `AuthedScreen` に `useLatestRef` を導入し、`handleRetryInitialLoad`/`handleRetryNextPageLoad`/`handleFavoriteFilterToggle`/`handleModeChange`/`handleTrashClick`/`handleOpenNoteCreate`/`handleEdit`/`handleMemoEditCancel`/`handleNoteModalOpenChange`/`handleNoteModalRequestClose` の参照を安定化。`Container1` を `React.memo` 化。`AuthedScreen.test.tsx` に「ノートモーダル開閉で一覧再取得しない」回帰ケースを追加。 | `PERF-01 baseline: render count=5.7, total render=133.2ms` | `PERF-01 after5: render count=5.0, total render=76.9ms`（run: total 92.1/68.0/70.6） | `render count -0.7 (-12.3%), total render -56.3ms (-42.3%)` | PERF-01達成。PERF-02/03再計測待ち | 2026-03-03 |

### Baseline / After 記入フォーマット

- `render count(avg)`: 数値
- `total render(ms, avg)`: 数値（全commit合計）
- 例: `render count=14.0, total render=320.5ms`

## 計測条件（再現手順）

### 共通条件

- データ条件: 未記録
- ログイン条件: 未記録
- ネットワーク条件: 未記録
- ブラウザ状態（キャッシュ有無など）: 未記録

### 計測手順

1. 計測対象画面を開く。
2. 現状実装で各シナリオを 3 回計測し、Baseline を記録する。
3. Profiler の `What caused this update?` と render 内訳で原因候補を絞る。
4. 改善候補を1件実施し、同一条件で再計測する。
5. 差分を算出し、次の改善候補の優先度を更新する。
6. PERF-01 はUX判定として「モード切替ボタン押下直後にアクティブ見た目が反映されるか」を確認する。

## シナリオ定義

| シナリオID  | 操作                     | 開始条件   | 完了条件              |
| ------- | ---------------------- | ------ | ----------------- |
| PERF-01 | モード切替（memo ↔ note）     | 一覧表示済み | 一覧UIが安定表示される      |
| PERF-02 | モーダル開閉（note composer）  | 一覧表示済み | 開閉アニメーション/描画が完了する |
| PERF-03 | 一覧操作（favorite切替/ページング） | 一覧表示済み | 一覧更新が完了する         |

### PERF-01 UX判定ルール（即時反映）

- 判定対象: モード切替ボタン（メモ/ノート/ごみ箱）のアクティブ見た目
- 期待値: 押下と同時に見た目が反映され、内部処理完了待ちにならない
- 例外: `hasEdits=true` の場合は離脱確認を優先し、見た目即時反映を行わない
- 記録方法: 各計測runで「即時反映: Pass/Fail」を記録する

## 計測結果（再レンダリング回数）

| シナリオID  | 指標           | Baseline（現状） | After（改善後） |  差分 | 判定（改善/悪化/同等） | メモ                                      |
| ------- | ------------ | -----------: | ---------: | --: | ------------ | --------------------------------------- |
| PERF-01 | render count |          5.7 |        5.0 | -0.7 | 改善           | Baseline: 7,7,3（平均5.7） / CYCLE-05 After: 7,4,4（平均5.0）。 |
| PERF-02 | render count |         14.0 |            |     | 未判定          | Run別commit数（1/mのm）: 14, 14, 14。平均 14.0。 |
| PERF-03 | render count |          8.7 |            |     | 未判定          | Run別commit数（1/mのm）: 11, 7, 8。平均 8.7。    |

## 計測結果（主要操作応答）

| シナリオID | 指標 | Baseline（現状） | After（改善後） | 差分 | 判定（改善/悪化/同等） | メモ |
| --- | --- | ---: | ---: | ---: | --- | --- |
| PERF-01 | response time (ms) | 133.2 | 76.9 | -56.3 | 改善（達成） | CYCLE-05 After total: 92.1, 68.0, 70.6（平均76.9）。Baseline比 -42.3%。 |
| PERF-02 | response time (ms) | 45.4 | | | 未判定 | 1 commit目のRender: 43.8ms, 48.2ms, 44.2ms（平均 45.4ms）。全commit合計は未集計。 |
| PERF-03 | response time (ms) | 51.9 | | | 未判定 | 1 commit目のRender: 55.8ms, 43.1ms, 56.7ms（平均 51.9ms）。全commit合計は未集計。 |

## 原因候補の絞り込み

| ID | 事象 | 根拠（Profiler/ログ） | 想定原因 | 優先度（High/Medium/Low） |
| --- | --- | --- | --- | --- |
| CAUSE-01 | モード切替時のrender時間が高め | 3回の1 commit目が40.4/52.0/57.4ms | モード切替に伴う描画範囲が広く、1 commitの負荷が高い | High |
| CAUSE-02 | ルーティング起因の更新が毎回発生 | `What caused this update?` が `AuthedScreen` + `Router` | URL状態更新に連動して `AuthedScreen` 配下が広く再評価される | High |
| CAUSE-03 | モーダル開閉でcommit数が多い | PERF-02 の各Runで `1/14`（=14 commits） | モーダル開閉時に再描画連鎖が発生し、更新回数が増えている | High |
| CAUSE-04 | 一覧操作の処理時間とcommit数のばらつきが大きい | PERF-03 の1 commit目が43.1〜56.7ms、commit数が7〜11 | キャッシュ状態や再評価対象の差で処理コストが不安定 | Medium |

## 分析メモ

- 主要な改善点: CYCLE-01/CYCLE-02で callback 参照安定化・描画境界分離・カード層 `React.memo` 化を実施。CYCLE-03でノートHTML処理の遅延実行 + キャッシュを追加
- 直近の状況: PERF-01 は CYCLE-03 After で total render 平均が 129.9ms（Baseline比 -2.5%）まで改善。render count も 5.7→4.0 に改善したが、主要KPI `<=106.5ms` は未達
- 想定要因: モード切替時の重い描画/計算コスト本体（ノート描画処理・一覧再計算）が依然支配的。CYCLE-04（可視領域優先 + idleキュー化）は `149.3ms` で悪化したため、現行は CYCLE-03 にロールバック済み
- CYCLE-05 実装状況: callback参照安定化 + `Container1` memo化を実装し、Jest 4 suites（91 tests）と Playwright 4 specs（23 tests）が通過
- CYCLE-05 計測状況: 実DBで PERF-01 の after5 を取得。`total render` 平均 76.9ms（92.1/68.0/70.6）で Baseline 133.2ms 比 -42.3%

## 次の最適化候補と優先度

| ID | 改善対象 | 仮説 | 期待効果 | 優先度（High/Medium/Low） | 対応方針 |
| --- | --- | --- | --- | --- | --- |
| OPT-01 | `AuthedScreen` の再描画境界 | サブツリー分離でモード切替時の影響範囲を縮小できる | render時間短縮 | High | 重い表示ブロックを `memo` 化しpropsを安定化 |
| OPT-02 | URL連動状態更新 | router更新と表示更新の結合が強い | commit数/再描画回数の削減 | High | 派生状態の計算位置を見直し、不要な再評価を抑制 |
| OPT-03 | ノートモーダル開閉時の更新連鎖 | 開閉トリガで不要な再評価が走っている | commit数削減、開閉体感改善 | High | モーダル関連stateを局所化し、親の再レンダリング波及を抑制 |
| OPT-04 | 一覧操作（favorite切替）時の更新安定化 | 一覧計算とURL更新が同時に走り負荷が変動している | 処理時間のばらつき抑制、体感改善 | Medium | favorite切替時の再計算経路を分離し、重い計算をメモ化 |

## 実行ログ（CYCLE-05）

- Jest:
  - `pnpm exec jest components/authed/__tests__/AuthedScreen.test.tsx components/authed/__tests__/useGuardedQueryNavigation.test.ts components/authed/__tests__/useNoteComposerState.test.ts components/authed/__tests__/useInfiniteLoadMore.test.ts --runInBand`
  - 結果: 4 suites / 91 tests pass
- Playwright:
  - `pnpm exec playwright test has-edits.spec.ts note-composer-history.spec.ts favorite-filter.spec.ts infinite-scroll.spec.ts`
  - 結果: 23 tests pass

## 完了判定

- 判定: [ ] 完了 / [ ] 未完
- 判定日:
- 判定者:
- 根拠:
