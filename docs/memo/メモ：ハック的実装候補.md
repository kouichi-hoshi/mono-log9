# ハック的実装候補メモ

作成日: 2026-03-04

## このメモが書かれた経緯

- F7-8（モード切替の体感速度改善）で、数値改善は限定的でもUX体感は大きく改善した。
- 一方で、改善のために `optimistic` や `timeout`、`ref` フラグ制御など実装難易度が上がる手法を導入した。
- 「今は効いているが、将来の改修で壊れやすい実装が混ざっていないか」を継続確認する必要があるため、ハック的実装候補を明示的に記録する目的で本メモを作成した。
- このメモは、今すぐ全置換するためではなく、今後の優先度判断（維持・改善・置換）に使う監視リストとして運用する。

## timeout で同期失敗を推定している箇所（準ハック）

- `mono-log9-app/components/authed/useOptimisticViewState.ts:71`
- `mono-log9-app/components/authed/AuthedScreen.tsx:251`
- `syncTimeoutMs=4000` で失敗判定するため、環境差で誤判定リスクがあります。

## ワンショット ref フラグで分岐制御している箇所（複雑化リスク）

- `mono-log9-app/components/authed/AuthedScreen.tsx:169`
- `mono-log9-app/components/authed/AuthedScreen.tsx:954`
- `skipDiscardConfirmOnNextNoteCloseRef` は意図的ですが、将来改修で壊れやすい典型パターンです。

## setTimeout(0) による描画遅延（最適化ハック寄り）

- `mono-log9-app/components/authed/PostCard.tsx:154`
- 効果はある一方で、タイミング依存の挙動になりやすいです。

## as unknown as で型を通している箇所（型安全の抜け道）

- `mono-log9-app/auth.ts:56`
- `mono-log9-app/app/api/auth/[...nextauth]/route.ts:41`
- `mono-log9-app/app/api/auth/[...nextauth]/route.ts:55`
