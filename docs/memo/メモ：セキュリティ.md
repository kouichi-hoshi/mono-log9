# セキュリティメモ（現状と未実装）

## 現在の実装状況

- [x] `dangerouslySetInnerHTML` のサニタイズ
  - 現在の対象は `mono-log9-app/components/authed/PostCard.tsx` のノート表示箇所。
  - `sanitizeRichHtml()` を通した HTML のみを `dangerouslySetInnerHTML` に渡している。

- [x] DOMPurify 実行失敗時の fail-closed 実装
  - `mono-log9-app/lib/sanitizeRichHtml.ts` の `resolvePurifier()` で解決不能時に `null` を返す。
  - `sanitizeRichHtml()` は purifier 未解決時に空文字 (`""`) を返す実装。

- [x] `href` 境界値（`data:`, `blob:`, `file:`）の検証
  - `mono-log9-app/lib/__tests__/sanitizeRichHtml.test.ts` で、上記スキームが除去されることを検証済み。

## 追加実装すべき内容（テスト強化）

- [ ] DOMPurify 失敗ケースの個別テスト追加（未実装）
  1. `window` がない実行環境（SSR/Node）での挙動を明示テストする
  2. `DOMPurify(window)` の戻り値が `sanitize` を持たないケースを明示テストする

- [ ] （任意）`dangerouslySetInnerHTML` 使用箇所の定期棚卸し
  - 新規追加時に `sanitizeRichHtml()` を必須化する運用チェックを入れる
