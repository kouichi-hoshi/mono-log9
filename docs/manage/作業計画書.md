---
title: 作業計画書
source:
author:
  -
published:
created: 2026-02-07
description:
tags:
---
# 作業計画書

|  項番 | フェーズ | チェックリスト（未/済） | タイトル                              | 実装内容                                                                                                                                                                                                                                                                                            | 進捗情報                                                                                      |
| --: | ---: | :----------: | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
|   1 |    1 |      未       | UI基盤/レイアウト骨格                      | `docs/specs/05.仕様書 UI.md` のHTML構造（header/main/article/footer）を実装。`docs/tech-stack/tech-stack.md` に従い Tailwind/shadcn/ui（button/dialog/alert/alert-dialog/popover/skeleton/sonner/checkbox）を導入し、アプリの共通レイアウト枠を用意（Miro: `ヘッダー`, `フッター`, `Link` を参照）                                                | 進行中（`button`/`dialog`/`sonner` と Toaster設置は完了。未ログイン画面の骨格は実装済み。残りの UI 部品導入とログイン中レイアウト骨格が未） |
|   2 |    1 |      済       | 未ログイン画面（ウェルカム/導線）                 | 未ログイン時の画面を実装（Miro: `未ログイン（sm/md共通）`）。ウェルカム文言・免責・Link仕様は `docs/specs/06.テキスト・コンテンツ定義.md` を正とする。ログインボタンでログインモーダルを開く（機能はスタブ）                                                                                                                                                                       | 完了（`/` に header/main(article)/footer、ウェルカム文言、Link 要件、ログイン導線まで実装）                          |
|   3 |    1 |      済       | ログインモーダルUI                        | ログイン用モーダルを実装（Miro: `モーダル`）。「Googleでログイン」ボタン、閉じる（×）、失敗/キャンセル時トースト表示（文言は `docs/specs/06`）までUIのみ作る（認証処理はスタブ）                                                                                                                                                                                       | 完了（モーダル開閉、×で閉じる、Googleボタンでエラートースト→閉じるまで実装）                                                |
|   4 |    1 |      済       | スタブ認証（URLトグル / USE_STUB_AUTH）による画面切替 | `docs/specs/07.仕様書 スタブ認証（URLトグル）.md` と `docs/specs/03.仕様書 環境変数 env.md` に従い、開発時のみ `stubAuth=1` をログイン中とみなし、未ログイン/ログイン中UIを切り替える。`USE_STUB_AUTH=true` の場合のみ有効化し、`NODE_ENV=test/production` では常に無効になるガードを入れる。ログイン操作は成功スタブで `stubAuth=1` を付与して遷移、ログアウトで削除。既存クエリ（例: `mode/view/favorite` 等）を保持する（設計: `docs/designs/スタブ認証（URLトグル）_設計.md`） | 完了（`lib/env.ts` にガード集約、`lib/stubAuth.ts` に判定/URL編集集約、`/` の表示切替・ログイン付与・ログアウト削除・他クエリ保持まで実装） |
|   5 |    1 |      未       | ヘッダーUI（ロゴ/状態表示）                   | ヘッダーの見た目と配置を実装（Miro: `ヘッダー`）。未ログイン/ログイン中で表示要素が切り替わる前提の骨格のみ作成                                                                                                                                                                                                                                    | 進行中（未ログイン表示は実装済み。ログイン中の表示切替前提の骨格が未）                                                       |
|   6 |    1 |      済       | ユーザーアイコン/ポップオーバーUI                | ユーザーアイコンとポップオーバーUIを実装（Miro: `ユーザーアイコン`, `ユーザアイコンのポップオーバー`）。ユーザー名表示・「ログアウト」導線を用意（処理はスタブ）                                                                                                                                                                                                         | 完了（`UserMenu` にユーザーアイコン、ポップオーバー、ユーザー名表示、ログアウト導線を実装）                                              |
|   7 |    1 |      未       | モード切替UI（メモ/ノート）                   | モード切替トグルを実装（Miro: `モード切替`）。仕様 `docs/specs/02.仕様書 機能.md` に従い、`view=trash` 中は非表示になる前提のUIを作る（状態はスタブ）                                                                                                                                                                                               | 未着手                                                                                       |
|   8 |    1 |      未       | ログイン中画面（スマホ）レイアウト                 | ログイン中のスマホレイアウトを実装（Miro: `ログイン中（sm）`）。タイムライン（投稿一覧）＋画面下部固定の投稿エディタを配置（データはスタブ）                                                                                                                                                                                                                     | 進行中（投稿一覧と投稿エディタの配置は実装済み。画面下部固定レイアウトは未実装）                                                     |
|   9 |    1 |      済       | ログイン中画面（md以上）レイアウト                | ログイン中のPC（md以上）レイアウトを実装（Miro: `ログイン中(md)`）。1カラムで、投稿一覧と投稿エディタを配置（データはスタブ）                                                                                                                                                                                                                         | 完了（1カラムのログイン中レイアウトと、投稿一覧＋投稿エディタの配置を実装）                                                        |
|  10 |    1 |      未       | 投稿エディタUI（メモ/ノート）                  | 投稿エディタUIを実装（Miro: `投稿エディタ`、ラベル例: 保存/全画面表示など）。`docs/specs/02` に従い、メモはinput、ノートはリッチテキスト（tiptap想定）＋拡大/縮小ボタン、保存/更新/キャンセルボタン等のUIを作成（保存はスタブ）                                                                                                                                                          | 未着手                                                                                       |
|  11 |    1 |      済       | 投稿カードUI（通常一覧）                     | 投稿カード（通常一覧の1件）を実装（Miro: `投稿個別`）。表示要素（作成日時/本文/編集/削除/スター）を配置し、ノートは「もっと見る/折りたたむ」UIまで作る（動作はスタブ）                                                                                                                                                                                                     | 完了（表示要素と「もっと見る/折りたたむ」UIまで実装。操作処理はスタブ）                                                          |
|  12 |    1 |      未       | 投稿検索UI（絞り込み/解除）                   | 絞り込みUIを実装（Miro: `投稿検索`）。お気に入り絞り込みトグル、絞り込み解除ボタンを配置（動作はスタブ）                                                                                                                                                                                                                                       | 進行中（お気に入り絞り込みトグルは実装済み。絞り込み解除の専用導線は未実装）                                                     |
|  13 |    1 |      未       | ごみ箱画面UI（sm/md）                    | ごみ箱画面のUIを実装（Miro: `ログイン中: ごみ箱（sm）`, `ログイン中: ごみ箱（md）`, `投稿個別（ごみ箱内）`）。一覧表示と「投稿一覧へもどる」導線を作成（データ/操作はスタブ）                                                                                                                                                                                            | 未着手                                                                                       |
|  14 |    1 |      未       | ごみ箱の一括操作UI                        | ごみ箱の一括操作UI（未選択/選択中の状態）を実装（Miro: `ごみ箱削除`, `ごみ箱削除（投稿チェック中）`）。チェック件数表示、選択削除、空にするボタン等のUIのみ（動作はスタブ）                                                                                                                                                                                                  | 未着手                                                                                       |
|  15 |    1 |      未       | 通知/アラート/ローディングUI                  | `docs/specs/06.テキスト・コンテンツ定義.md` の成功/エラー文言を用いたトースト/アラートUIを整備。無限スクロール待機時のスケルトン、空表示（「投稿がありません。」等）もUIとして用意                                                                                                                                                                                          | 進行中（空表示・スケルトン・一部トーストは実装済み。成功/エラー文言の整備は未完）                                                |
|  16 |    2 |      未       | URLクエリ状態管理（mode/view/favorite）    | `docs/specs/02` の方針に従い、画面状態をURLパラメータで管理（モード切替/ごみ箱/絞り込み）。ブラウザ戻る/進むで状態復元できるようにする（実装方針のみでも可）                                                                                                                                                                                                       | 未着手                                                                                       |
|  17 |    2 |      未       | スタブ投稿データ（USE_STUB_POSTS）          | `USE_STUB_POSTS` に従い、開発用スタブの投稿/お気に入りデータ層を用意。`test/production` では無効化し、Repository切替ポイントを明確化                                                                                                                                                                                                       | 未着手                                                                                       |
|  18 |    2 |      未       | 投稿作成/更新（バリデーション込み）                | `docs/specs/02` に従い保存/更新を実装。空入力不可、最大文字数（memo=280/note=20000）、成功/失敗通知（文言は `docs/specs/06`）を適用。保存後のクリア/フォーカスも反映                                                                                                                                                                                   | 未着手                                                                                       |
|  19 |    2 |      未       | ごみ箱投入/復元（単体操作）                    | 投稿の削除は「ごみ箱に移動」として実装し、復元も実装（`docs/specs/02`）。通常一覧からの削除（ごみ箱投入）で即座に非表示になること                                                                                                                                                                                                                        | 未着手                                                                                       |
|  20 |    2 |      未       | TanStack Query：一覧取得/キャッシュ/無限スクロール | `docs/specs/02` のキャッシュ方針（通常/ごみ箱、staleTime=5分/gcTime=30分、refetchOnMount/Focus/Reconnect=false）で実装。無限スクロールの追加取得とスケルトン表示を整備                                                                                                                                                                        | 未着手                                                                                       |
|  21 |    2 |      未       | 投稿検索（favorite絞り込み）                | `docs/specs/02` の仕様どおり、favoriteで絞り込めるようにする。絞り込み中は再取得する挙動を実装                                                                                                                                                                                                                                     | 未着手                                                                                       |
|  22 |    2 |      未       | hasEdits離脱確認                      | `docs/specs/02` の `hasEdits` 仕様を実装。URLが変わる操作（モード/絞り込み/ごみ箱切替等）、他投稿編集開始、モーダルclose、戻る/進む/リロード/タブクローズで確認を出し、破棄/継続を選べるようにする                                                                                                                                                                          | 未着手                                                                                       |
|  23 |    2 |      未       | ごみ箱の一括選択/完全削除                     | `docs/specs/02` に従い、ロード済み分のみ一括チェック、追加ロード分は自動チェックしない。選択件数表示、選択した投稿の完全削除、空にする（全件）を実装。`view=trash` 出入りで選択状態リセット                                                                                                                                                                                    | 未着手                                                                                       |
|  24 |    2 |      未       | エラー/再試行/401時ログイン誘導                | `docs/specs/02` のエラーハンドリングに従い、401はトースト＋入力維持のままログインモーダル自動表示、403/404は状態維持、500はエラー通知＋再試行導線（「再試行」）を提供                                                                                                                                                                                               | 未着手                                                                                       |
|  25 |    2 |      未       | PWA manifest / icons              | `docs/specs/04.仕様書 設定 PWA.md` に従い manifest（start_url/scope/display/theme_color等）を実装。`public/icons/` に 192/512 + maskable を配置し、オフライン対応（SW）は初期リリース対象外                                                                                                                                             | 未着手                                                                                       |
|  26 |    3 |      未       | Auth.js（Googleログイン）本実装            | `docs/specs/01`/`02` に基づき Auth.js のGoogleログインを実装。`docs/specs/03` の env（AUTH_SECRET/AUTH_URL/GOOGLE_CLIENT_*）を前提にし、ログイン/ログアウトのUI連動を完成させる                                                                                                                                                         | 未着手                                                                                       |
|  27 |    3 |      未       | Prisma/DB設計（投稿/関連）                | `docs/specs/02` のデータ要件（投稿モード固定、favorite、ごみ箱、trashedAt順など）を満たす Prisma スキーマを設計し、Repository/APIの土台を作る（DBマイグレーション実行は別タスクで管理）                                                                                                                                                                        | 未着手                                                                                       |
|  28 |    3 |      未       | API/Repository実装（認可込み）            | 投稿一覧取得（通常/ごみ箱/絞り込み）、作成/更新/ごみ箱投入/復元/完全削除のAPIとRepositoryを実装。ユーザー単位の認可・エラーコード（401/403/404）を担保                                                                                                                                                                                                      | 未着手                                                                                       |
|  29 |    3 |      未       | スタブ→実DB切替とガード                     | `USE_STUB_AUTH`/`USE_STUB_POSTS` の切替を整理し、test/productionでは必ずスタブが無効になるガードを徹底。実装箇所を集約して誤設定時の事故を防ぐ                                                                                                                                                                                                 | 未着手                                                                                       |
|  30 |    3 |      未       | Jest（単体/結合）テスト基盤と主要ケース            | `docs/specs/02` のコア仕様（投稿バリデーション、hasEdits、URL状態、favorite絞り込み、権限/エラー）を中心に Jest で単体/結合テストを整備。TDD運用の土台を作る                                                                                                                                                                                           | 未着手                                                                                       |
|  31 |    4 |      未       | Playwright E2E（主要フロー）             | ログイン→投稿作成/編集→絞り込み→ごみ箱→復元/完全削除のE2EをPlaywrightで整備し、回帰の軸を作る                                                                                                                                                                                                                                        | 未着手                                                                                       |
|  32 |    4 |      未       | CWV計測/目標達成の確認                     | `docs/specs/01`/`02` のCWV目標（LCP/CLS/INP）を満たすか、PageSpeed Insights と Vercel Speed Insights で確認する手順/チェックをタスク化し、改善ループを回す                                                                                                                                                                            | 未着手                                                                                       |
