---
title: 作業計画書
source:
author:
  -
published:
created: 2026-02-07
description:
tags:
---
# 作業計画書

|   項番 | フェーズ | チェックリスト（未/済） | タイトル                                       | 実装内容                                                                                                                                                                                                                                                                                                                                                                      | 進捗情報                                                                                                                                                                                        |
| ---: | ---: | :----------: | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|    1 |    1 |      済       | UI基盤/レイアウト骨格                               | `docs/specs/05.仕様書 UI.md` のHTML構造（header/main/article/footer）を実装。`docs/tech-stack/tech-stack.md` に従い Tailwind/shadcn/ui（button/dialog/alert/alert-dialog/popover/skeleton/sonner/checkbox）を導入し、アプリの共通レイアウト枠を用意（Miro: `ヘッダー`, `フッター`, `Link` を参照）                                                                                                                          | 完了（shadcn/ui 対象部品の導入、Toaster 設置、未ログイン/ログイン中のレイアウト骨格を実装）                                                                                                                                     |
|    2 |    1 |      済       | 未ログイン画面（ウェルカム/導線）                          | 未ログイン時の画面を実装（Miro: `未ログイン`）。ウェルカム文言・免責・Link仕様は `docs/specs/06.テキスト・コンテンツ定義.md` を正とする。ログインボタンでログインモーダルを開く（機能はスタブ）                                                                                                                                                                                                                                                          | 完了（`/` に header/main(article)/footer、ウェルカム文言、Link 要件、ログイン導線まで実装）                                                                                                                            |
|    3 |    1 |      済       | ログインモーダルUI                                 | ログイン用モーダルを実装（Miro: `モーダル`）。「Googleでログイン」ボタン、閉じる（×）、失敗/キャンセル時トースト表示（文言は `docs/specs/06`）までUIのみ作る（認証処理はスタブ）                                                                                                                                                                                                                                                                 | 完了（モーダル開閉、×で閉じる、Googleボタンでエラートースト→閉じるまで実装）                                                                                                                                                  |
|    4 |    1 |      済       | スタブ認証（URLトグル / USE_STUB_AUTH）による画面切替       | `docs/specs/07.仕様書 スタブ認証（URLトグル）.md` と `docs/specs/03.仕様書 環境変数 env.md` に従い、開発時のみ `stubAuth=1` をログイン中とみなし、未ログイン/ログイン中UIを切り替える。`USE_STUB_AUTH=true` の場合のみ有効化し、`NODE_ENV=test/production` では常に無効になるガードを入れる。ログイン操作は成功スタブで `stubAuth=1` を付与して遷移、ログアウトで URL パラメータを全削除して `/` へ戻す。ログイン時は既存クエリ（例: `view/favoriteMemo/favoriteNote` 等）を保持する（設計: `docs/designs/スタブ認証（URLトグル）_設計.md`） | 完了（`lib/env.ts` にガード集約、`lib/stubAuth.ts` に判定/URL編集集約、`/` の表示切替・ログイン付与・ログアウト時URL全削除・他クエリ保持まで実装）                                                                                              |
|    5 |    1 |      済       | ヘッダーUI（ロゴ/状態表示）                            | ヘッダーの見た目と配置を実装（Miro: `ヘッダー`）。未ログインはサービスロゴ表示、ログイン中はサービスロゴ非表示 + モード切替/ユーザーUI表示の前提で作成                                                                                                                                                                                                                                                                                        | 完了（未ログイン: ロゴ表示、ログイン中: ロゴ非表示 + モード切替/ユーザーUI表示を実装）                                                                                                                                            |
|    6 |    1 |      済       | ユーザーアイコン/ポップオーバーUI                         | ユーザーアイコンとポップオーバーUIを実装（Miro: `ユーザーアイコン`, `ユーザアイコンのポップオーバー`）。ユーザー名表示・「ログアウト」導線を用意（処理はスタブ）                                                                                                                                                                                                                                                                                   | 完了（`UserMenu` にユーザーアイコン、ポップオーバー、ユーザー名表示、ログアウト導線を実装）                                                                                                                                         |
|    7 |    1 |      済       | モード切替UI（メモ/ノート）                            | モード切替トグルを実装（Miro: `モード切替`）。仕様 `docs/specs/02.仕様書 機能.md` に従い、メモ/ノートの切り替えUIを作る（状態はスタブ）                                                                                                                                                                                                                                                                                      | 完了（メモ/ノートのトグルUIを実装）                                                                                                                                                                         |
|    8 |    1 |      済       | 投稿エディタUI（メモ/ノート）                           | 投稿エディタUIを実装（Miro: `メモエディタ`, `ノートエディタ`）。`docs/specs/02` に従い、メモはinput、ノートはフルスクリーンモーダル内のエディタ（tiptap想定）として、保存/更新/キャンセルなどのUIを作成（保存はスタブ）                                                                                                                                                                                                                                        | 完了（TipTap最小導入、メモ/ノートUI分離、投稿カード編集導線、保存/更新/キャンセル導線、Jest/RTLテストを実装）                                                                                                                            |
|    9 |    1 |      済       | 投稿カードUI（通常一覧）                              | 投稿カード（通常一覧の1件）を実装（Miro: `投稿個別`）。表示要素（作成日時/本文/編集/削除/スター）を配置し、ノートは「もっと見る/折りたたむ」UIまで作る（動作はスタブ）                                                                                                                                                                                                                                                                               | 完了（表示要素と「もっと見る/折りたたむ」UIまで実装。操作処理はスタブ）                                                                                                                                                       |
|   10 |    1 |      済       | 投稿検索UI（絞り込み/解除）                            | 絞り込みUIを実装（Miro: `投稿検索`）。お気に入りボタンのトグルで絞り込みON/OFF（解除）を行う（動作はスタブ）                                                                                                                                                                                                                                                                                                            | 完了（お気に入りトグルによる絞り込みON/OFFを実装）                                                                                                                                                                |
|   11 |    1 |      済       | ごみ箱画面UI                                    | ごみ箱画面のUIを実装（Miro: , `ログイン中: ごみ箱`）。一覧表示を作成（データ/操作はスタブ）                                                                                                                                                                                                                                                                                                                     | 完了（ごみ箱一覧表示、表示切り替えUI3ボタンの排他的アクティブ制御、モード復帰導線、関連Jestテストを実装）                                                                                                                                    |
|   12 |    1 |      済       | ごみ箱の一括操作UI                                 | ごみ箱の一括操作UI（未選択/選択中の状態）を実装（Miro: `ごみ箱削除`, `ごみ箱削除（投稿チェック中）`）。チェック件数表示、選択削除、空にするボタン等のUIのみ（動作はスタブ）                                                                                                                                                                                                                                                                            | 完了（選択/解除トグル、n件選択中表示、一括削除/空にする確認モーダル分岐、通常画面復帰時の選択リセット、関連Jestテストを実装）                                                                                                                          |
|   13 |    1 |      済       | 通知/アラート/ローディングUI                           | 空表示（「投稿がありません。」）と通知/アラートのUIを整備する。フェーズ1では操作処理がスタブのため、トースト文言はプレースホルダー（「未実装です」）を許容し、機能実装時（フェーズ2）に `docs/specs/06.テキスト・コンテンツ定義.md` の仕様文言へ置換する                                                                                                                                                                                                                                 | 完了（空表示・通知/アラートUIを実装。スタブ操作時のプレースホルダー文言はフェーズ2で仕様文言へ置換）                                                                                                                                        |
|   14 |    2 |      済       | TipTapカスタマイズ（ツールバー/Markdown運用）             | `docs/specs/08.仕様書 Tiptap.md` に従い、TipTapのツールバー（見出し: H2/H3/H4、リスト: 箇条書き/番号付き、太字、リンク）を実装する。Markdownはショートカット入力（`#`/`##`、`- `/`1. `、`**text**`）で設定できるようにし、エディタおよび投稿カードでmd風スタイル（`.tiptap h1/h2`, `.tiptap ul/ol`, `.tiptap strong`, `.tiptap a`）を適用する。投稿カードのHTML描画は `DOMPurify` を導入してサニタイズした上で表示する（Markdown文字列の保存・入出力は対象外）                                                     | 完了（ツールバー/リンクダイアログ、Markdownショートカット、DOMPurify適用、関連テストまで実装）                                                                                                                                    |
|   15 |    2 |      済       | URLクエリ状態管理（view/favoriteMemo/favoriteNote） | `docs/specs/02` の方針に従い、画面状態をURLパラメータで管理（モード切替/ごみ箱/絞り込み）。ブラウザ戻る/進むで状態復元できるようにする（実装方針のみでも可）。※離脱確認（hasEdits）は本タスクに含めず、項番21で実装する                                                                                                                                                                                                                                              | 完了（`AuthedScreen` をURL単一ソース化、初期正規化`replace`/操作`push`/no-op非更新、`stubAuth`等他クエリ保持、ログアウトURLを`/`へ統一、関連Jestテスト追加。`favoriteNote` のURL直編集時に発生する hydration mismatch を `PostCard` のSSR/CSR描画方針調整で解消） |
|   16 |    2 |      済       | スタブ投稿データ（USE_STUB_POSTS）                   | `USE_STUB_POSTS` に従い、開発用スタブの投稿/お気に入りデータ層を用意。`test/production` では無効化し、Repository切替ポイントを明確化。参照: `docs/specs/10.仕様書 スタブ投稿データ（USE_STUB_POSTS）.md` / `docs/designs/スタブ投稿データ（USE_STUB_POSTS）_設計.md`                                                                                                                                                                             | 完了（`postRepository` に切替集約、`stubPostRepository` に一覧/カーソル/書き込みI/F・冪等性・エラー契約を実装、関連Jestテスト通過）                                                                                                   |
|   17 |    2 |      済       | ノート保存形式定義（TipTap）                          | TipTapノート本文の保存形式をJSONに固定し、`contentText` の役割、create/update入出力契約、バリデーション/エラー契約を仕様化する。詳細設計（導出アルゴリズム/処理順序）は `docs/designs/投稿編集-本文形式_設計.md` を正とする。参照: `docs/specs/12.仕様書 投稿編集-本文形式.md`                                                                                                                                                                                         | 完了（`PostRecord`/create/update を TipTap JSON + `contentText` 契約へ統一、`content` スキーマ検証と `contentText` 導出/文字数検証を実装）                                                                              |
|   18 |    2 |      済       | 投稿作成/更新（バリデーション込み）                         | `docs/specs/02` に従い保存/更新を実装。空入力不可、最大文字数（memo=280/note=25000）、成功/失敗通知（文言は `docs/specs/06`）を適用。保存後のクリア/フォーカスも反映                                                                                                                                                                                                                                                             | 完了（`AuthedScreen` を create/update 実処理へ接続、成功/失敗トースト、保存成功時クリア/フォーカス、memo/note 編集更新を実装）                                                                                                        |
|   19 |    2 |      済       | ごみ箱投入/復元（単体操作）                             | 投稿の削除は「ごみ箱に移動」として実装し、復元も実装（`docs/specs/02`）。通常一覧からの削除（ごみ箱投入）で即座に非表示になること                                                                                                                                                                                                                                                                                                  | 完了（通常一覧の削除で `moveToTrashAction` 実行+即時除外、ごみ箱一覧の復元で `restoreFromTrashAction` 実行+即時除外、関連テストを追加）                                                                                                |
|   20 |    2 |      済       | TanStack Query：一覧取得/キャッシュ/無限スクロール          | `docs/specs/02` のキャッシュ方針（通常/ごみ箱、staleTime=5分/gcTime=30分、refetchOnMount/Focus/Reconnect=false）で実装。無限スクロールの追加取得と、初回/追加読み込み時のスケルトン表示制御を整備                                                                                                                                                                                                                                    | 完了（`QueryProvider` 導入、`usePostsInfiniteQuery`/`queryKey` 分離、IntersectionObserver 無限スクロール、初回/追加ロードの表示分離、追加取得失敗時の再試行導線、一覧条件+履歴エントリ単位のスクロール復元、関連Jest/Playwrightテスト追加）                          |
|   21 |    2 |      済       | hasEdits離脱確認                               | `docs/specs/13.仕様書 投稿編集・離脱確認.md` を正として `hasEdits` を実装。URLが変わる操作（モード/絞り込み/ごみ箱切替等）、他投稿編集開始、Esc押下、戻る/進むで確認を出し、破棄/継続を選べるようにする                                                                                                                                                                                                                                   | 完了（`AuthedScreen` に離脱確認を一元化、`lib/posts/hasEdits.ts` を追加、`NoteComposerModal` を dirty 通知/close 要求型へ変更、Unit/Jest/Playwright テストを追加） |
|   22 |    2 |      済       | 投稿検索（favorite絞り込み）                         | `docs/specs/02` の仕様どおり、favoriteで絞り込めるようにする。切替先の一覧条件が未取得の場合のみ再取得し、取得済みの場合はキャッシュを即時復元する挙動を実装                                                                                                                                                                                                                                                                                | 完了（`AuthedScreen` の favorite更新を同一`view`関連キャッシュ同期へ変更し、`favoriteOnly=true/false` 双方向の整合を保証。fallback invalidationを追加。Jest: `AuthedScreen.test.tsx` + `authedQueryState/queryKeys` 通過、Playwright: `favorite-filter.spec.ts` 新規4件通過、`has-edits.spec.ts`/`infinite-scroll.spec.ts` 回帰通過） |
| 22-A |    2 |      済       | ノートモーダル履歴連動（戻る/進む時の挙動統一）                   | 仕様: ノート作成/編集モーダル表示中にブラウザの戻る/進むを実行した場合、背後一覧の遷移より先にモーダル状態のみを変化させる（戻る=モーダルを閉じる、進む=モーダルを再表示）。実装: モーダル開閉状態をURLクエリで管理し、開く操作は履歴を1件積む。閉じる操作はクエリ差分で反映する。未保存変更がある場合は項番21（hasEdits離脱確認）を適用し、破棄確認を経由してから状態遷移させる。Jest（結合）とPlaywright UIテストを追加して回帰を防止する。仕様書を更新する。                                                                                                                       | 完了（`noteComposer=create/edit` のURL連動、戻る/進む時のモーダル復元、dirty時の保留遷移・破棄確認を実装。保存直後に誤って破棄確認が出る不具合を修正。Jest: `AuthedScreen.test.tsx`、Playwright: `note-composer-history.spec.ts` と `note-heading-save.spec.ts` の回帰ケースを追加） |
|   23 |    2 |      済       | ごみ箱の一括選択/完全削除                              | `docs/specs/02` に従い、ロード済み分のみ一括チェック、追加ロード分は自動チェックしない。選択件数表示、選択した投稿の完全削除、空にする（全件）を実装。`view=trash` 出入りで選択状態リセット                                                                                                                                                                                                                                                              | 完了（Repository/action/UIを接続し、行単位削除・選択削除・空にするを実装。削除後の全`posts`キャッシュ整合と `view=trash` 離脱時の選択リセットを反映。失敗時は一括削除モーダルを閉じず再試行可能、行削除も再押下で再試行可能に修正。Jest/Playwright: `AuthedScreen.test.tsx` / `trash-delete.spec.ts`） |
|   24 |    2 |      済       | エラー/再試行/401時ログイン誘導                         | `docs/specs/02` のエラーハンドリングに従い、401はトースト＋入力維持のままログインモーダル自動表示、403/404は状態維持、500はエラー通知＋再試行導線（「再試行」）を提供                                                                                                                                                                                                                                                                         | 完了（`UNAUTHORIZED/INTERNAL_ERROR` を含むエラーハンドラを導入し、401時にログインモーダル自動表示＋入力保持、403/404/500相当はトーストのみで状態維持を実装。初回取得失敗「再試行」/追加取得失敗「追加取得を再試行」/削除失敗再試行を確認。Jest: `AuthedScreen.test.tsx`、`postActions.test.ts`） |
|   25 |    2 |      未       | PWA manifest / icons                       | `docs/specs/04.仕様書 設定 PWA.md` に従い manifest（start_url/scope/display/theme_color等）を実装。`public/icons/` に 192/512 + maskable を配置し、オフライン対応（SW）は初期リリース対象外                                                                                                                                                                                                                       | 未着手                                                                                                                                                                                         |
|   26 |    3 |      未       | Auth.js（Googleログイン）本実装                     | `docs/specs/01`/`02`/`14` に基づき Auth.js のGoogleログインを実装。`docs/specs/03` の env（AUTH_SECRET/AUTH_URL/GOOGLE_CLIENT_*）を前提にし、ログイン/ログアウトのUI連動、callbackUrl（`stubAuth`除去）、ログアウト時キャッシュ破棄を完成させる。テスト観点は `docs/test-cases/項番26_Auth.jsGoogleログイン本実装_テストケース.md` を正とする                                                                                                                                                                   | 未着手（仕様確定: `docs/specs/14.仕様書 Auth.js（Googleログイン）導入.md` を追加）                                                                                                                                                                                         |
|   27 |    3 |      未       | Prisma/DB設計（投稿/関連）                         | `docs/specs/02` のデータ要件（投稿モード固定、favorite、ごみ箱、trashedAt順など）を満たす Prisma スキーマを設計し、Repository/APIの土台を作る（DBマイグレーション実行は別タスクで管理）                                                                                                                                                                                                                                                  | 未着手                                                                                                                                                                                         |
|   28 |    3 |      未       | TipTapノート永続化（DB/API接続）                     | 項番16で定義した保存形式に従い、ノート本文の作成/取得/更新をAPI/Repositoryへ接続する。Prismaスキーマに保存フィールドを反映し、一覧/詳細/編集の各経路で同一形式で扱えることを担保する                                                                                                                                                                                                                                                                   | 未着手                                                                                                                                                                                         |
|   29 |    3 |      未       | API/Repository実装（認可込み）                     | 投稿一覧取得（通常/ごみ箱/絞り込み）、作成/更新/ごみ箱投入/復元/完全削除のAPIとRepositoryを実装。ユーザー単位の認可・エラーコード（401/403/404）を担保                                                                                                                                                                                                                                                                                | 未着手                                                                                                                                                                                         |
|   30 |    3 |      未       | スタブ→実DB切替とガード                              | `USE_STUB_AUTH`/`USE_STUB_POSTS` の切替を整理し、test/productionでは必ずスタブが無効になるガードを徹底。実装箇所を集約して誤設定時の事故を防ぐ                                                                                                                                                                                                                                                                           | 未着手                                                                                                                                                                                         |
|   31 |    3 |      未       | Jest（単体/結合）テスト基盤と主要ケース                     | `docs/specs/02` のコア仕様（投稿バリデーション、hasEdits、URL状態、favorite絞り込み、権限/エラー）を中心に Jest で単体/結合テストを整備。TDD運用の土台を作る                                                                                                                                                                                                                                                                     | 未着手                                                                                                                                                                                         |
|   32 |    4 |      未       | Playwright E2E（主要フロー）                      | ログイン→投稿作成/編集→絞り込み→ごみ箱→復元/完全削除のE2EをPlaywrightで整備し、回帰の軸を作る                                                                                                                                                                                                                                                                                                                  | 未着手                                                                                                                                                                                         |
|   33 |    4 |      未       | CWV計測/目標達成の確認                              | `docs/specs/01`/`02` のCWV目標（LCP/CLS/INP）を満たすか、PageSpeed Insights と Vercel Speed Insights で確認する手順/チェックをタスク化し、改善ループを回す                                                                                                                                                                                                                                                      | 未着手                                                                                                                                                                                         |
