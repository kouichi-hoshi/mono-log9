---
title: 項番32 Playwright E2E（主要フロー）残タスク
source:
  - docs/manage/作業計画書.md
  - docs/specs/仕様書 機能.md
  - docs/specs/仕様書 Auth.js（Googleログイン）導入.md
  - docs/test-cases/項番26_Auth.jsGoogleログイン本実装_テストケース.md
  - docs/test-cases/項番27_PrismaDB設計_テストケース.md
  - docs/test-cases/項番28_TipTapノート永続化_テストケース.md
  - docs/test-cases/項番31_Jest単体結合テスト基盤と主要ケース_テストケース.md
created: 2026-02-25
description: 項番32を完了するための残タスク一覧。作業計画書の「Playwright E2E（主要フロー）」を整備し、回帰の軸を作る。
tags:
  - manage
  - e2e
  - phase4
---

# 項番32 残タスク

## 背景

- **作業計画書**: フェーズ4、項番32「Playwright E2E（主要フロー）」は現状**進行中（未完了）**
- **目的**: ログイン→投稿作成/編集→絞り込み→ごみ箱→復元/完全削除のE2EをPlaywrightで整備し、回帰の軸を作る
- **現状**: Playwright UIテストはスタブ環境（`USE_STUB_AUTH=true` / `USE_STUB_POSTS=true`）で稼働。`major-flow.stub.spec.ts` と `logout-history-bfcache.spec.ts` を追加し、`chromium` で 37 テストが通過。実DB・実認証の主要フロー（T32-05〜T32-08）は未完了

---

## 残タスク一覧

### 1. 事前準備

| ID | タスク | 内容 | 優先度 |
|----|--------|------|--------|
| T32-01 | テストケースドキュメント作成 | `docs/test-cases/項番32_Playwright E2E主要フロー_テストケース.md` を新規作成し、対象・非対象・完了判定・TC一覧を定義する | 高 |
| T32-02 | 実行環境設計 | スタブ環境 vs 実DB+実認証の切り替え方針を決定する（例: Playwright `projects` で分離、環境変数で `webServer` を切り替え） | 高 |

### 2. 主要フローE2E（スタブ環境）

| ID | タスク | 内容 | 参照 |
|----|--------|------|------|
| T32-03 | 総合シナリオ spec 実装 | ログイン（stubAuth=1）→メモ作成→ノート作成→編集→favorite絞り込み→ごみ箱投入→ごみ箱→復元→再度ごみ箱→完全削除の一連フローを1 spec で実装する | 作業計画書 |
| T32-04 | 回帰実行手順の整備 | 既存の `pnpm test:e2e` を前提に、T32-02で追加する `projects` を含めた実行手順（stubのみ / 実DB+実認証のみ / 全件）を `docs/test-cases/項番32_Playwright E2E主要フロー_テストケース.md` に明記する。実Google OAuth画面操作を含むケースは `@oauth-manual` 等で分離し、デフォルト実行から除外する | 項番31同様 |

### 3. 本番相当E2E（実DB・実認証）

| ID | タスク | 内容 | 参照 |
|----|--------|------|------|
| T32-05 | 実DB経路での投稿作成〜表示 | ローカルPostgreSQL + Auth.js ログイン状態で、ログイン→作成→一覧確認までのE2Eを実装する（Google画面操作の自動化は必須にしない） | 項番27 TC-025 |
| T32-06 | 401→再ログイン導線 | 401再現可能な設定で、入力→保存→401発生→再ログイン→再実行可能をE2Eで検証する（Google画面操作の自動化は必須にしない） | 項番27 TC-026, 項番26 TC-010 |
| T32-07 | ログイン後の状態保持 | `/?view=note&favoriteNote` でログイン後もクエリ保持・`stubAuth` 除去を検証する（Google画面操作の自動化は必須にしない） | 項番26 TC-008 |
| T32-08 | ログアウトで状態初期化 | ログアウト後 URL が `/` になり、ログイン中UIに戻らないことを検証する（Google画面操作の自動化は必須にしない） | 項番26 TC-009 |

### 4. ログアウト後・履歴復元・BFCache

| ID | タスク | 内容 | 参照 |
|----|--------|------|------|
| T32-09 | ログアウト後の戻る/進む検証 | ログアウト後にブラウザの戻る/進むを実行したときの履歴復元挙動を検証・確定する | 仕様書 機能 |
| T32-10 | BFCache差異の厳密評価 | ログアウト後の戻る/進む時の BFCache 表示差異を評価する（まず Chromium を自動化し、必要に応じて Firefox/WebKit を追加。Safari実機差分は手動確認） | 項番26, 仕様書 Auth.js |

### 5. 完了判定・ドキュメント更新

| ID | タスク | 内容 | 参照 |
|----|--------|------|------|
| T32-11 | 完了判定の満足確認 | 項番32テストケースの必須TCが実装済みかつ Pass であることを確認する | 項番31 同様 |
| T32-12 | 作業計画書更新 | 項番32のチェックリストを「済」、進捗情報を完了内容に更新する | 作業計画書 |
| T32-13 | CI 連携（任意） | GitHub Actions 等で Playwright を定期実行する設定を追加する（項番32の完了必須条件には含めない） | - |

---

## 依存関係

```
T32-01（テストケース作成）
    ↓
T32-02（実行環境設計）
    ↓
T32-03（総合シナリオ） ← 即着手可能（スタブ環境既存）
T32-04（回帰実行手順）← T32-03 と並行可

T32-05〜T32-08（本番相当） ← T32-02 完了後、実DB/実認証環境が前提
T32-09〜T32-10（BFCache等） ← 必要に応じ T32-05 以降と並行

T32-11〜T32-12 ← 全実装完了後に実施
```

---

## 非対象（項番32スコープ外）

- CWV 計測・性能目標の達成確認（項番33）
- PWA manifest / icons（項番25）
- スタブ環境のみの個別機能テスト（個別 spec は既存で充足。項番32は「総合フロー」が主軸）

---

## 実認証E2E運用方針（項番32）

- 方針: 「アプリ内品質は自動化」「Google OAuth 画面遷移は手動スモーク」で運用する
- 自動化対象（T32-05〜T32-08）:
  - 実DB（PostgreSQL）と Auth.js 境界を使った主要導線（作成/更新/401再ログイン/状態保持/ログアウト）
  - CIを導入する場合は、デフォルト対象に含める
- 手動スモーク対象:
  - Google のアカウント選択/同意画面を実際に操作するフロー
  - テストコードは作成してよいが、`@oauth-manual` 等で分離し、CIデフォルト実行には含めない
- 実DB E2Eの安全実行前提:
  - `RUN_REAL_E2E=true` と `ALLOW_DB_WRITE_FOR_TESTS=true` を同時に指定する
  - `TEST_DB_HOST_ALLOWLIST` / `TEST_DB_NAME_ALLOWLIST` にテストDB接続先を明示し、許可外DBへの接続は fail-fast で停止する

---

## 実行環境の現状

| 項目 | 現状 |
|------|------|
| 実行コマンド | `mono-log9-app` で `pnpm test:e2e`（全Playwrightを一括実行可能） |
| 実DB E2Eガード | `RUN_REAL_E2E` + `ALLOW_DB_WRITE_FOR_TESTS` + allowlist 指定が必須（未指定時は開始前停止） |
| `playwright.config.ts` | 通常は `NEXT_DIST_DIR=.next-playwright PORT=3100 USE_STUB_AUTH=true USE_STUB_POSTS=true pnpm dev`。`RUN_REAL_E2E=true` 時のみ実認証向け設定へ切替 |
| `projects` | 通常は `chromium`。`RUN_REAL_E2E=true` 時は `real-chromium` を追加 |
| 実DB/実認証での Playwright | real project + globalSetup ガードを実装済み。`major-flow.real.spec.ts` は `@oauth-manual` 雛形（実フロー実装は T32-05〜T32-08 で継続） |

項番32の本番相当E2E（T32-05〜T32-08）を行うには、`RUN_REAL_E2E=true` と安全ガード用envを明示した上で、テスト用 Google OAuth / ローカル PostgreSQL を準備する。
