
- dangerouslySetInnerHTML関連箇所全て、サニタイズできているか？
- DOMPurifyの実行失敗するケース
	- resolvePurifier() が null を返す状態です。
		1. DOMPurify の import 結果に sanitize がない
		    - 実際に起きた default.sanitize is not a function のケースです。
		2. window がない実行環境
		    - SSR/プリレンダ/Node実行中など。
		    - このとき DOMPurify がファクトリ型だと初期化できず null になります。
		3. DOMPurify(window) しても sanitize を持つインスタンスが取れない

- href の境界値（data:, blob:, file:）検証するテストはあるか？